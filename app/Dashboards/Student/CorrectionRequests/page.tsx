import { RoutePlaceholder } from "@/app/_Components/routePlaceholder";

export default function StudentCorrectionRequestsPage() {
  return (
    <RoutePlaceholder
      title="Correction requests"
      description="Review your attendance correction requests."
    />
  );
}
