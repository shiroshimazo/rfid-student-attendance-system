import { RoutePlaceholder } from "@/app/_Components/routePlaceholder";

export default function TeacherProfilePage() {
  return (
    <RoutePlaceholder
      title="Profile"
      description="View and update your teacher profile."
    />
  );
}
