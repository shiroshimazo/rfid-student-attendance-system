import { RoutePlaceholder } from "@/app/_Components/routePlaceholder";

export default function RfidCardsPage() {
  return (
    <RoutePlaceholder
      title="RFID cards"
      description="View registered cards and assignment status."
    />
  );
}
