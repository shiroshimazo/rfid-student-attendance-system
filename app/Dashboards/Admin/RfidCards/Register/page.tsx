import { RoutePlaceholder } from "@/app/_Components/routePlaceholder";

export default function RegisterRfidCardPage() {
  return (
    <RoutePlaceholder
      title="Register RFID card"
      description="Register and assign a new RFID card."
    />
  );
}
