import { RoutePlaceholder } from "@/app/_Components/routePlaceholder";

export default function NewAnnouncementPage() {
  return (
    <RoutePlaceholder
      title="New announcement"
      description="Publish an announcement to selected audiences."
    />
  );
}
