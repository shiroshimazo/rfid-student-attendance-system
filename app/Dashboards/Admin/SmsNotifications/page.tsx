import { RoutePlaceholder } from "@/app/_Components/routePlaceholder";

export default function SmsNotificationsPage() {
  return (
    <RoutePlaceholder
      title="SMS notifications"
      description="Review delivery status and notification history."
    />
  );
}
