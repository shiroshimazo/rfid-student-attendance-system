import { RoutePlaceholder } from "@/app/_Components/routePlaceholder";

export default function AdminCorrectionRequestsPage() {
  return (
    <RoutePlaceholder
      title="Correction requests"
      description="Review requested attendance corrections."
    />
  );
}
