import { RoutePlaceholder } from "@/app/_Components/routePlaceholder";

export default function AdminSettingsPage() {
  return (
    <RoutePlaceholder
      title="Settings"
      description="Configure attendance, notification, and system preferences."
    />
  );
}
