import { RoutePlaceholder } from "@/app/_Components/routePlaceholder";

export default function AdminDashboardPage() {
  return (
    <RoutePlaceholder
      title="Admin dashboard"
      description="Manage attendance operations, people, devices, and system activity."
    />
  );
}
